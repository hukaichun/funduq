# Working on funduq

- Develop with uv. Point at an env file with `uv run --env-file <path> ...`.
- A bad smell found mid-implementation usually means the architecture is
  wrong. Stop and discuss; do not push through to done.
